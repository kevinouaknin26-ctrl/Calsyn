node_modules
dist
.env.local
*.log

# Migration data dumps — contain client data + Twilio SIDs, never commit
migrations/**/prod/*.json
migrations/**/restore/*.json
migrations/**/merge.sql

# Playwright MCP artefacts (snapshots, audio extraits) — locaux uniquement
.playwright-mcp/
